export const ABILITY_SPELL_PROFILE_JSON_EDITOR_VIEW_CONTRACT_VERSION =
  "ability_spell_profile_json_editor_view_contract_v1";

/**
 * Portable LOOM JSON editor boundary. The view receives complete JSON text,
 * display-ready validation issues, status text, and semantic callbacks only.
 * Persistence and actor runtime state remain outside this view contract.
 */
