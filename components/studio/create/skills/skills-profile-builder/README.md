# Skills Profile Builder

LOOM create flow:

```text
SkillsProfileBuilderShell
→ useSkillsProfileBuilderViewModel
→ SkillsProfileBuilderView
→ SkillsProfileEditor
→ skillsClient
→ existing /api/creations proxy
```

Create route: `/studio/create/skills-profile`.
