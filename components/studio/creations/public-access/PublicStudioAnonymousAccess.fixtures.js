export const publicStudioAnonymousCommunityFixture = Object.freeze({
  authenticated: false,
  surface: "COMMUNITY",
});

export const publicStudioAnonymousCreationFixture = Object.freeze({
  authenticated: false,
  surface: "CREATION",
});

export const publicStudioAnonymousProfileFixture = Object.freeze({
  authenticated: false,
  surface: "PROFILE",
});

export const publicStudioAuthenticatedCommunityFixture = Object.freeze({
  authenticated: true,
  surface: "COMMUNITY",
});

export const publicStudioAnonymousProtectedFixture = Object.freeze({
  authenticated: false,
  surface: null,
});

export const publicStudioAuthenticatedPrivateFixture = Object.freeze({
  authenticated: true,
  surface: null,
});
