export const cameraPresetDefaultFixture = Object.freeze({
  selectedValue: "MEDIUM_WIDE_SHOT",
  searchValue: "",
});

export const cameraPresetLegacyValueFixture = Object.freeze({
  selectedValue: "THREE_QUARTER",
  searchValue: "",
});

export const cameraPresetSearchFixture = Object.freeze({
  selectedValue: "DEEP_FOCUS",
  searchValue: "focus",
});

export const cameraPresetEmptySearchFixture = Object.freeze({
  selectedValue: "AUTO",
  searchValue: "no matching camera treatment",
});

export const cameraPresetSpecializedFixture = Object.freeze({
  selectedValue: "VERTIGO_SHOT",
  searchValue: "vertigo",
});
