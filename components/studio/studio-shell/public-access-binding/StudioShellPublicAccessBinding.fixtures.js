export const studioShellPublicAccessAnonymousCommunityFixture =
  Object.freeze({
    authenticated: false,
    surface: "COMMUNITY",
  });

export const studioShellPublicAccessAnonymousCreationFixture =
  Object.freeze({
    authenticated: false,
    surface: "CREATION",
  });

export const studioShellPublicAccessAnonymousProfileFixture =
  Object.freeze({
    authenticated: false,
    surface: "PROFILE",
  });

export const studioShellPublicAccessAuthenticatedCommunityFixture =
  Object.freeze({
    authenticated: true,
    surface: "COMMUNITY",
  });

export const studioShellPublicAccessAuthenticatedPrivateFixture =
  Object.freeze({
    authenticated: true,
    surface: null,
  });

export const studioShellPublicAccessAnonymousProtectedFixture =
  Object.freeze({
    authenticated: false,
    surface: null,
  });
