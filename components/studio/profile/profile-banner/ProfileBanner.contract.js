export const PROFILE_BANNER_VIEW_CONTRACT_VERSION = "1.0.0";

/**
 * Stable UI boundary for the portable profile-banner primitive.
 *
 * The View owns banner presentation, compact and regular heights, image
 * rendering, accessible image text, and the empty banner-slot state. It does
 * not own profile loading, media generation, upload, selection, persistence,
 * permissions, moderation, API calls, or profile editing.
 *
 * @typedef {Object} ProfileBannerViewProps
 * @property {string|null} bannerUrl
 * @property {string} title
 * @property {boolean} compact
 */

export {};
